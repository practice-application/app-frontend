const url = 'http://localhost:8080'

export const config = {
    'goService': {
        peopleApi: `${url}/people`,
        orgApi: `${url}/organisation`,
        productApi: `${url}/products`,
    }

}

